# いらないアセット消しちゃうもんネーター

VRChat のアバター改変用 Unity プロジェクトから、**どのアバターからも参照されていない未使用アセット**（買ったまま着せていない衣装、試して外した髪型、使わなかったテクスチャなど）を検出し、**安全に退避**してディスク容量を取り戻す Unity Editor 拡張です。

> **削除ではなく「退避」が既定です。** 退避したアセットはプロジェクトルート直下のフォルダへ移動されるだけで、いつでも元に戻せます。誤検出でデータを失わないことを最優先に設計しています。

📖 **[使い方ドキュメント](https://docs.maaaaa.net/Asset-Keshichaumon-Nator/)** — 画面ごとの操作手順・保護のしくみ・トラブル対処

---

## 動作環境

| 項目 | 要件 |
|---|---|
| Unity | 2022.3 (LTS) |
| VRChat SDK | Avatars 3.7.0 以上（アバターの自動検出に使用。無くても手動指定で動作） |
| OS | 全機能: Windows。キャッシュ掃除機能のみ Windows 専用 |

---

## インストール（VPM）

1. VCC / ALCOM にリポジトリを追加します。
2. プロジェクトの Manage Packages から「いらないアセット消しちゃうもんネーター」を追加します。

導入すると Unity のメニューに `Tools > いらないアセット消しちゃうもんネーター` が追加されます。

VCC を使っていない場合は、[リリースページ](https://github.com/Poyotoron/Asset-Keshichaumon-Nator/releases)の `.unitypackage` からも導入できます。

---

## できること

| 機能 | 概要 |
|---|---|
| 未使用アセットの検出・退避 | 登録したアバターから辿れないアセットを検出し、`.meta` ごと退避フォルダへ移動します |
| 復元 | 退避したものを元の場所へ戻します。参照も復活します |
| 保護 | ツール・シェーダー・コード資産などを自動で除外します。判定に迷うものは消さずに保護します |
| 重複検出 | 内容が完全に一致するファイルを検出します（レポートのみ） |
| キャッシュ掃除 | `Library/` `Temp/` を削除して容量を取り戻します（Windows 専用） |

**具体的な操作手順・画面の読み方・誤検出したときの対処は、[使い方ドキュメント](https://docs.maaaaa.net/Asset-Keshichaumon-Nator/)を参照してください。**

---

## 安全のための設計

- **既定は退避（移動）で、削除ではありません。** `.meta` ごと移動するので、戻せば参照も復活します。
- **迷ったら保護。** 消し損ね（未使用なのに残る）は無害ですが、誤削除は購入資産を壊します。判定に迷うものは保護側に倒します。
- **スキャンは常に非破壊。** 実際にファイルが動くのは、明示的に退避ボタンを押したときだけです。
- 文字列パスやコード経由の参照など、依存関係の解析で見えない参照もあり得ます。だからこそ「削除」ではなく「退避」を既定にしています。

---

## 不具合の報告

ツールの **情報 (About)** タブから報告先を開けます。環境情報をコピーするボタンもあります。

**とくに「使っているアセットなのに候補に出た」という報告を最優先で受け付けます。** → [GitHub Issues](https://github.com/Poyotoron/Asset-Keshichaumon-Nator/issues)

---

## ライセンス

MIT License. 詳細は [LICENSE](LICENSE) を参照してください。
